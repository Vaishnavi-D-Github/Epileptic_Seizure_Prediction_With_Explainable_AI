import { useNavigate } from "react-router-dom";
import { ParticleBackground } from "@/components/ParticleBackground";

const ExampleReports = () => {
  const navigate = useNavigate();

  return (
    <div className="relative min-h-screen overflow-hidden bg-[#020617] text-white">
      <ParticleBackground />

      <div className="relative z-10 px-6 md:px-20 py-20 flex flex-col gap-12 items-center text-center">
        <div className="space-y-4 max-w-3xl">
          <h1 className="text-4xl md:text-5xl font-bold text-cyan-300 drop-shadow-[0_0_25px_rgba(0,255,255,0.45)]">
            Example Model Reports by Confidence Range
          </h1>
          <p className="text-lg text-gray-300">
            Explore example outputs generated by the AI model for different confidence intervals. Each section demonstrates the prediction label, SHAP visualization, and interpretation.
          </p>
        </div>

        {/* Example Sections */}
        <section className="w-full max-w-5xl space-y-12 text-left">
          {/* Very High Certainty */}
          <div className="bg-white/5 border border-cyan-400/30 rounded-3xl px-8 py-10 backdrop-blur-lg shadow-[0_0_35px_rgba(0,255,255,0.2)]">
            <h2 className="text-2xl font-semibold text-blue-300">90% – 100% Confidence (Very High Certainty)</h2>
            <p className="text-gray-300 mt-3">
              The model is almost certain this EEG represents a seizure pattern. Bright red SHAP regions highlight
              synchronized spike-and-wave bursts typical of epileptic activity.
            </p>
            <img
              src="/src/reports/high-confidence.png"
              alt="High confidence report example"
              className="rounded-xl mt-6 border border-cyan-500/30"
            />
          </div>

          {/* High Confidence */}
          <div className="bg-white/5 border border-cyan-400/30 rounded-3xl px-8 py-10 backdrop-blur-lg shadow-[0_0_35px_rgba(0,255,255,0.2)]">
            <h2 className="text-2xl font-semibold text-blue-300">70% – 89% Confidence (High Confidence)</h2>
            <p className="text-gray-300 mt-3">
              The AI predicts a seizure with strong probability, but the waveform may contain mild artifacts. SHAP
              highlights localized spikes in red and calm areas in blue.
            </p>
            <img
              src="/src/reports/moderate-confidence.png"
              alt="High confidence report example"
              className="rounded-xl mt-6 border border-cyan-500/30"
            />
          </div>

          {/* Moderate Confidence */}
          <div className="bg-white/5 border border-cyan-400/30 rounded-3xl px-8 py-10 backdrop-blur-lg shadow-[0_0_35px_rgba(0,255,255,0.2)]">
            <h2 className="text-2xl font-semibold text-blue-300">50% – 69% Confidence (Moderate)</h2>
            <p className="text-gray-300 mt-3">
              The waveform partially resembles seizure patterns. SHAP shows mixed regions — red and blue overlapping —
              indicating uncertainty.
            </p>
            <img
              src="/src/reports/medium-confidence.png"
              alt="Moderate confidence example"
              className="rounded-xl mt-6 border border-cyan-500/30"
            />
          </div>

        </section>

        <button
          onClick={() => navigate("/detailed-reports")}
          className="px-8 py-3 rounded-xl border border-gray-600 bg-white/5 hover:bg-white/10 text-gray-100 font-semibold transition-colors"
        >
          ← Back to Reports
        </button>
      </div>
    </div>
  );
};

export default ExampleReports;
